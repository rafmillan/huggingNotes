import { EditorProps } from "@tiptap/pm/view";

//This is the "text areas"
export const TiptapEditorProps: EditorProps = {
  attributes: {
    class:
      "prose-lg prose-headings:font-display focus:outline-none",
  },
};
